<table class="table">
  <thead>
    <tr>
      <th scope="col" style="width: 5%">n°</th>
      <th scope="col" style="width: 20%">Mail's list</th>
      <th scope="col" style="width: 75%">Mail's content</th>
    </tr>
  </thead>
  <tbody>
    <%@mails.each do |mail|%>
    <tr id="row<%=mail.id%>" class='row<%=mail.read%>'>
      <th scope="row"><%=mail.id%></th>
      <td><%=link_to "#{mail.object}", mail_path(:id => mail.id), :remote => true, :class => "button"%></td>
      <td><div class='text', id='text<%=mail.id%>' style="display:none;"></div></td>
    </tr>
    <%end%>
  </tbody>
</table>
